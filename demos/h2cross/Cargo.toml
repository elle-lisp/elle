[package]
name = "h2cross"
version = "0.1.0"
edition = "2024"

[dependencies]
h2 = "0.4"
tokio = { version = "1", features = ["full"] }
bytes = "1"
clap = { version = "4", features = ["derive"] }
http = "1"

[workspace]
