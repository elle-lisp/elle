[package]
name = "elle-myplugin"
version = "1.0.0"
edition = "2021"
description = "The worked example of docs/cookbook/plugins.md, built so the literate documents that load it run"

[lib]
crate-type = ["cdylib"]

[dependencies]
elle-plugin = { path = "../../elle-plugin" }
