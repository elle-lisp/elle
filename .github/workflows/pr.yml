name: PR

on:
  pull_request:
    branches: [ main, develop ]
    types: [ opened, synchronize, reopened, labeled, unlabeled ]

concurrency:
  group: pr-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1

jobs:
  changes:
    name: Detect Changes
    runs-on: ubuntu-latest
    outputs:
      source: ${{ steps.filter.outputs.source }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Check for source changes
        id: filter
        run: |
          BASE="${{ github.event.pull_request.base.sha }}"
          CHANGED=$(git diff --name-only "$BASE" HEAD)
          echo "Changed files:"
          echo "$CHANGED"
          if echo "$CHANGED" | grep -qE '\.(rs|lisp)$|^Cargo\.|^Makefile$|^\.cargo/'; then
            echo "source=true" >> "$GITHUB_OUTPUT"
          else
            echo "source=false" >> "$GITHUB_OUTPUT"
          fi

  qa:
    name: QA (fmt, clippy, doc)
    needs: changes
    runs-on: ubuntu-latest
    if: needs.changes.outputs.source == 'true'
    env:
      MLIR_SYS_220_PREFIX: /usr/lib/llvm-22
      TABLEGEN_220_PREFIX: /usr/lib/llvm-22
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt,clippy
      - uses: Swatinem/rust-cache@v2
      - name: Install LLVM 22 (for mlir feature)
        run: |
          wget -qO- https://apt.llvm.org/llvm-snapshot.gpg.key | sudo tee /etc/apt/trusted.gpg.d/apt.llvm.org.asc
          echo "deb http://apt.llvm.org/$(lsb_release -cs)/ llvm-toolchain-$(lsb_release -cs)-22 main" | sudo tee /etc/apt/sources.list.d/llvm.list
          sudo apt-get update
          sudo apt-get install -y llvm-22-dev mlir-22-tools libmlir-22-dev
      - name: Check formatting
        run: cargo fmt -- --check
      - name: Run clippy on workspace
        run: cargo clippy --workspace --all-targets --all-features -- -D warnings
      - name: Check documentation
        run: cargo doc --no-deps --document-private-items
        env:
          RUSTDOCFLAGS: "-D warnings"

  check-plugin-list:
    name: Plugin List Sync
    needs: changes
    runs-on: ubuntu-latest
    if: needs.changes.outputs.source == 'true'
    steps:
      - uses: actions/checkout@v4
      - name: Verify PLUGINS matches Cargo.toml
        run: make check-plugin-list

  build-plugins:
    name: Build Plugin (${{ matrix.plugin }})
    needs: changes
    runs-on: ubuntu-latest
    if: needs.changes.outputs.source == 'true'
    strategy:
      fail-fast: false
      matrix:
        plugin:
          - arrow
          - crypto
          - csv
          - hash
          - jiff
          - msgpack
          - oxigraph
          - polars
          - protobuf
          - random
          - regex
          - selkie
          - syn
          - tls
          - toml
          - tree-sitter
          - xml
          - yaml
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: plugin-${{ matrix.plugin }}
      - name: Build plugin
        run: cargo build --release -p elle-${{ matrix.plugin }}
      - name: Upload plugin .so
        uses: actions/upload-artifact@v4
        with:
          name: plugin-${{ matrix.plugin }}
          path: target/release/libelle_${{ matrix.plugin }}.so
          retention-days: 1

  tests:
    name: Elle Tests
    needs: changes
    runs-on: ubuntu-latest
    if: needs.changes.outputs.source == 'true'
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - name: Install GNU parallel and Redis
        run: |
          sudo apt-get update
          sudo apt-get install -y parallel redis-server libsqlite3-dev libz-dev libzstd-dev libgit2-dev
          sudo systemctl start redis-server
      - name: Build release binary
        run: cargo build --release -p elle
      - name: Run examples and Elle script tests
        run: make smoke

  plugin-tests:
    name: Plugin Tests
    needs: [changes, build-plugins]
    runs-on: ubuntu-latest
    if: needs.changes.outputs.source == 'true'
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - name: Build release binary
        run: cargo build --release -p elle
      - name: Download all plugin artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: plugin-*
          path: target/release/
          merge-multiple: true
      - name: Run plugin tests
        run: make plugin-tests

  tests-combined:
    name: Integration & Property Tests
    needs: changes
    runs-on: ubuntu-latest
    if: needs.changes.outputs.source == 'true'
    env:
      PROPTEST_CASES: 8
      RUST_TEST_THREADS: 2
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - name: Run integration tests
        run: cargo test --test '*' -- --skip property
      - name: Run property tests
        run: "cargo test --test lib property::"

  mlir:
    name: MLIR Backend Tests
    needs: changes
    runs-on: ubuntu-latest
    if: needs.changes.outputs.source == 'true'
    env:
      MLIR_SYS_220_PREFIX: /usr/lib/llvm-22
      TABLEGEN_220_PREFIX: /usr/lib/llvm-22
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: mlir
      - name: Install LLVM 22 and GNU parallel
        run: |
          wget -qO- https://apt.llvm.org/llvm-snapshot.gpg.key | sudo tee /etc/apt/trusted.gpg.d/apt.llvm.org.asc
          echo "deb http://apt.llvm.org/$(lsb_release -cs)/ llvm-toolchain-$(lsb_release -cs)-22 main" | sudo tee /etc/apt/sources.list.d/llvm.list
          sudo apt-get update
          sudo apt-get install -y llvm-22-dev mlir-22-tools libmlir-22-dev parallel
      - name: Build with MLIR
        run: cargo build --release -p elle --features mlir
      - name: Run MLIR and SPIR-V tests
        run: |
          ./target/release/elle tests/elle/mlir.lisp
          ./target/release/elle tests/elle/spirv.lisp

  aarch64:
    name: AArch64 Build & Smoke
    needs: changes
    runs-on: ubuntu-24.04-arm
    if: needs.changes.outputs.source == 'true'
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: aarch64
      - name: Install GNU parallel
        run: |
          sudo apt-get update
          sudo apt-get install -y parallel
      - name: Build release binary
        run: cargo build --release -p elle
      - name: Run smoke tests
        run: make smoke

  macos:
    name: macOS Build & Smoke
    needs: changes
    runs-on: macos-latest
    if: needs.changes.outputs.source == 'true'
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: macos
      - name: Install GNU parallel and coreutils
        run: brew install parallel coreutils
      - name: Build release binary
        run: cargo build --release -p elle
      - name: Run smoke tests
        run: make smoke

  all-checks:
    name: All Checks Passed
    needs: [changes, qa, check-plugin-list, tests-combined, build-plugins, tests, plugin-tests, mlir, aarch64, macos]
    runs-on: ubuntu-latest
    if: always()
    steps:
      - name: Check QA
        if: needs.qa.result == 'failure'
        run: exit 1
      - name: Check plugin list
        if: needs.check-plugin-list.result == 'failure'
        run: exit 1
      - name: Check combined tests
        if: needs.tests-combined.result == 'failure'
        run: exit 1
      - name: Check plugin builds
        if: needs.build-plugins.result == 'failure'
        run: exit 1
      - name: Check tests
        if: needs.tests.result == 'failure'
        run: exit 1
      - name: Check plugin tests
        if: needs.plugin-tests.result == 'failure'
        run: exit 1
      - name: Check MLIR
        if: needs.mlir.result == 'failure'
        run: exit 1
      - name: Check aarch64
        if: needs.aarch64.result == 'failure'
        run: exit 1
      - name: Check macOS
        if: needs.macos.result == 'failure'
        run: exit 1
      - name: Check changes detection
        if: needs.changes.result == 'failure'
        run: exit 1
      - name: Success
        run: echo "All checks passed"
